#===============================================================================
# LDAP 密码过期通知 — 配置文件模板
# 复制为 password_expiry_notify.env 并填入真实值：
#   cp password_expiry_notify.env.example password_expiry_notify.env
#   chmod 600 password_expiry_notify.env
#
# 该文件含敏感信息，已在 .gitignore 中忽略，切勿提交到代码库。
# 配置优先级：环境变量 > 本文件 > 脚本内置默认值。
#===============================================================================

#------------------------------------------------------------------------------
# LDAP 连接（示例占位符，请替换为真实值）
#------------------------------------------------------------------------------
LDAP_MASTER1=ldap01.example.com
LDAP_MASTER2=ldap02.example.com
LDAPS_PORT=636
LDAP_SUFFIX=dc=example,dc=com
LDAP_RO_DN=cn=readonly,dc=example,dc=com
LDAP_RO_PW=CHANGE_ME
LDAP_TLS_CACERT=/path/to/ldap/ldap_certs/ca.crt
LDAP_USER_BASE=ou=People,dc=example,dc=com

#------------------------------------------------------------------------------
# 通知阈值：未来多少天内密码过期（默认 15 天）
#------------------------------------------------------------------------------
NOTIFY_DAYS=15

#------------------------------------------------------------------------------
# SMTP 邮件服务器（端口 25，无加密）
#------------------------------------------------------------------------------
SMTP_HOST=mail.example.com
SMTP_PORT=25
SMTP_USER=donotreply@example.com
SMTP_PASS=CHANGE_ME
SMTP_FROM=donotreply@example.com
SMTP_FROM_NAME=LDAP密码过期通知

#------------------------------------------------------------------------------
# 汇总邮件收件人
#------------------------------------------------------------------------------
ADMIN_TO=it@example.com
ADMIN_CC=admin@example.com

#------------------------------------------------------------------------------
# 自助改密网站地址
#------------------------------------------------------------------------------
SELF_SERVICE_URL=https://self.example.com/
